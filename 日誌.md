# SaveMoney 開發交接日誌

更新日期：2026-05-06

## 專案定位

SaveMoney 是一個記帳軟體專案，目標是未來開發成：

- 手機 App
- 網頁版
- 可選的桌面 App

重要修正：本專案不是要基於 Termux 執行，而是要做出類似 Termux 的視覺與操作體驗。也就是 terminal-inspired UI：緊湊、文字優先、快速操作、支援類 Termux extra keys 的快捷鍵盤。

目前 Python CLI 只是用來驗證記帳邏輯、資料模型與輸入流程的原型，不是最終產品形態。

## Repo 與路徑

GitHub：

```text
https://github.com/teri16/savemoney
```

本機路徑：

```text
C:\Users\Maxsu\OneDrive\文件\New project
```

主要檔案：

- `money.py`：目前 Python CLI / TUI 原型
- `README.md`：專案入口與同步手冊
- `docs/accounting-commands.md`：記帳指令操作教學
- `docs/gui-design.md`：手機 App / 網頁版 GUI 設計方向
- `prototypes/terminal-inspired-ui.html`：靜態 HTML 介面原型
- `日誌.md`：AI 交接用開發日誌

## 目前已完成

### 記帳核心原型

- SQLite 本地資料庫
- 新增收入與支出
- 列出交易紀錄
- 月報表
- 文字長條圖
- 圓餅比例表
- 範例資料 `demo`

主要指令：

```bash
python money.py add -120 早餐 -c food -a cash
python money.py add 52000 薪水 -c salary -a bank
python money.py list
python money.py report
python money.py demo
```

### 記憶輸入

已支援記憶舊有：

- 備註
- 分類
- 帳戶

查詢指令：

```bash
python money.py notes
python money.py categories
python money.py accounts
```

### 輸入模式

目前有三種輸入方式：

- `add`：一般 CLI 指令輸入
- `menu`：選單式輸入，較適合中文輸入
- `quick`：全畫面 quick entry 原型，含虛擬鍵盤概念

推薦中文輸入優先使用：

```bash
python money.py menu
```

### GUI / UX 設計

已確認方向：

- 手機 App 與網頁版是主要目標
- Termux-like 是視覺與操作風格，不是 runtime
- 需要內建類 Termux extra keys 的快捷鍵盤
- 手機端應使用原生輸入欄位，支援中文輸入法
- 快捷鍵盤是 App 內 UI 元件，可水平滑動、可依欄位切換

預設快捷鍵：

```text
ESC TAB ← → DEL NOTE CAT ACCT TODAY SAVE
food transport shopping salary cash bank card
```

## 重要設計決策

1. 不把 Termux 當執行環境。
2. 保留 terminal-inspired 視覺風格。
3. 手機 App 必須支援中文輸入法，不能依賴 terminal key event。
4. 網頁版與手機版要共用相同產品概念：Quick / Ledger / Report / Memory / Settings。
5. Python CLI 只作為原型與資料邏輯驗證。
6. UI 不做行銷式首頁，第一畫面就是可用的記帳介面。
7. 視覺風格偏深色 console、緊湊、功能導向。

## 最近 commit 歷史

```text
e54fecd Add Termux-like shortcut keyboard design
e94db46 Clarify mobile web UI direction
e32f2fb Add menu entry mode
f6c53cc Add GUI design guide
148dbc1 Add remembered inputs and quick entry
e9afcb4 Add accounting command guide
70eae26 Add bilingual sync manual
ab7ca29 Initial MoneyTerm prototype
```

## 已知限制

- 本機 Windows 目前沒有可用的 `python` 指令，因此尚未在本機實跑 Python smoke test。
- `quick` 使用 curses，全畫面中文輸入在部分環境可能不穩。
- 最終手機 App / 網頁版尚未開始真正框架實作，目前只有 HTML 靜態原型。
- README 早期曾有部分中文在 PowerShell 顯示亂碼，GitHub 通常以 UTF-8 顯示較正常；後續可視情況重整文件編碼與內容。

## 建議下一步

優先順序建議：

1. 決定手機 / 網頁技術路線，例如 React + Capacitor、React Native、Flutter、或 SvelteKit。
2. 將 `prototypes/terminal-inspired-ui.html` 轉成真正可互動的前端原型。
3. 建立資料模型層，保留 transactions / accounts / categories。
4. 實作 Quick Entry：
   - 原生文字輸入
   - 金額觸控鍵盤
   - 類 Termux 快捷鍵盤列
   - 舊備註、分類、帳戶建議
5. 實作 Ledger 列表與 Report 圖表。
6. 加入匯出 / 備份功能。

## 給下一位 AI 的注意事項

- 使用者偏好繁體中文溝通。
- 使用者想要的是手機 App / 網頁版，不是 Termux CLI 工具。
- 不要再把 Termux 描述成最終執行平台。
- 可以保留「像 Termux」的操作風格與快捷鍵盤概念。
- 若要做前端，請先查看 `docs/gui-design.md` 與 `prototypes/terminal-inspired-ui.html`。
- 若要同步 GitHub，repo 已設定 `origin`，branch 是 `master`。
- 本機 `.git` 寫入常需要提升權限。
- 推送 GitHub 通常也需要提升權限，因為一般 sandbox 無法連線。
